# Kali only publishes its cloud image as a .tar.xz of a sparse raw disk. CDI
# decompresses the xz and writes the tar onto the PVC as the disk. DataVolume
# says Succeeded, VM has nothing to boot. Verified 2026-09.
#
# So: download once, verify the checksum, unpack with GNU tar (busybox chokes on
# the sparse entry), convert to qcow2, serve it over HTTP. kali-golden imports
# from here. emptyDir on purpose; the only consumer is the golden bake.
#
# New Kali release: change URL and SHA256 (from .../current/SHA256SUMS), then
# `kubectl delete vm kali-golden -n vms`.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kali-image
  namespace: vms
  annotations:
    argocd.argoproj.io/sync-wave: "-1"   # be Ready before the golden's import starts
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: kali-image
  template:
    metadata:
      labels:
        app: kali-image
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
      initContainers:
        - name: fetch-and-convert
          image: alpine:3.20
          env:
            - name: URL
              value: https://kali.download/cloud-images/kali-2026.2/kali-linux-2026.2-cloud-genericcloud-amd64.tar.xz
            - name: SHA256
              value: 3f4ca86ae1eca0dc2bf2092f065a959508e186c6b44f85758813a8e9a0604f3e
          command:
            - sh
            - -ec
            - |
              apk add -q --no-cache tar xz qemu-img
              cd /srv
              if [ -f kali.qcow2 ]; then echo "already converted"; exit 0; fi
              echo "downloading $URL"
              wget -q -O kali.tar.xz "$URL"
              echo "$SHA256  kali.tar.xz" | sha256sum -c
              echo "unpacking"
              xz -dc kali.tar.xz | tar -x
              rm -f kali.tar.xz
              raw=$(ls *.raw | head -1)
              echo "converting $raw"
              qemu-img convert -f raw -O qcow2 "$raw" kali.qcow2.tmp
              rm -f "$raw"
              mv kali.qcow2.tmp kali.qcow2
              ls -lh /srv
          volumeMounts:
            - name: srv
              mountPath: /srv
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /kali.qcow2
              port: 80
              httpHeaders:
                - name: Range
                  value: bytes=0-0
          volumeMounts:
            - name: srv
              mountPath: /usr/share/nginx/html
              readOnly: true
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
      volumes:
        - name: srv
          emptyDir:
            sizeLimit: 30Gi
---
apiVersion: v1
kind: Service
metadata:
  name: kali-image
  namespace: vms
  annotations:
    argocd.argoproj.io/sync-wave: "-1"   # be Ready before the golden's import starts
spec:
  selector:
    app: kali-image
  ports:
    - port: 80
      targetPort: 80
